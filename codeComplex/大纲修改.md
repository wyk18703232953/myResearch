## 3. 方法：基于运行时采样的经验复杂度推断

### 3.1 方法概述
- 输入：代码片段 + 输入规模参数标识
- 输出：预测的复杂度类 + 时间函数表达式
- 整体流程图

### 3.2 输入规模识别与代码转换
- **大语言模型辅助的代码转换**：使用LLM将原始代码转换为`main(n)`形式的可执行函数，其中n为输入规模参数
- **转换策略**：通过提示工程指导LLM完成代码重构，确保转换后的代码保持原始复杂度特征
- **局限性讨论**：对非标准输入格式的处理限制

### 3.3 自适应采样策略
- **问题**：如何在有限的采样次数内覆盖有代表性的输入规模
- **策略详述**（举例）：
  - 初始采样：对数间隔采样（如 1, 2, 4, 8, 16, 32, ...）
  - 动态调整：根据初步拟合结果，在曲率大的区域增加采样点
  - 终止条件：拟合优度稳定或达到最大采样次数
- **与朴素采样（均匀采样）的对比**
<!-- 此处需要修改 -->

### 3.4 模拟执行与数据收集
- 执行环境设置（如何控制变量、减少噪声）
- 多次运行取最小值
- 处理超时/内存溢出（设置上限）

### 3.5 函数拟合
- **候选函数族**：
  - O(1): T(n) = c
  - O(log n): T(n) = a·log(n) + b
  - O(n): T(n) = a·n + b
  - O(n log n): T(n) = a·n·log(n) + b
  - O(n²): T(n) = a·n² + b
  - O(n³): T(n) = a·n³ + b
  - O(2ⁿ): T(n) = a·2ⁿ + b
- **拟合算法**：鲁棒回归

### 3.6 函数选择
- **评估指标**：R²、AIC/BIC
- **选择策略**：选最优指标对应的函数族
- **置信度输出**：给出拟合优度作为预测置信度

### 3.7 复杂度输出
- 输出格式：复杂度类 + 具体函数 + 置信度

### 3.8 方法复杂度分析
- 自身的时间/空间复杂度
- 采样次数 vs. 准确率的权衡

---

## 4. 实验设置

### 4.1 数据集
- 延世大学构建的数据集（描述其规模、来源、代码类型）
- 本实验使用的子集/全部
- 数据集的统计信息（代码长度分布、复杂度类分布）

### 4.2 对比方法
- **基线1：本文方法的朴素版本**（如均匀采样+线性拟合）
- **基线2：本文方法的完整版本**（自适应采样+多函数族拟合）
- **对比方法1：延世大学机器学习方法**（具体说明是哪个：RL? 对比学习? CodeT5?）
- **对比方法2：GPT-4o**（提示词设计、实验设置）
- **可选对比**：其他LLM（如Claude、Llama）

### 4.3 评估指标
- **准确率**：复杂度类预测正确比例
- **混淆矩阵**：分析哪些类容易混淆
- **拟合优度**：R²（对本方法内部评估）
- **运行时间**：各方法的预测耗时
- **资源消耗**：CPU/GPU、内存

### 4.4 实验环境
- 硬件配置
- 软件版本
- 随机种子设置

---

## 5. 实验结果与分析

### 5.1 本文方法内部对比（第一个对比）
- **实验目的**：验证本文方法各组件有效性
- **对比设置**：
  - M1: 均匀采样 + 多函数族拟合
  - M3: 自适应采样 + 多函数族拟合
- **结果**：
  - 准确率: M0 (xx%) < M1 (xx%) < M2 (xx%) < M3 (55%)
  - 分析：自适应采样贡献了xx个百分点，多函数族拟合贡献了xx个百分点
- **结论**：本文方法的各设计选择都带来了实质改进

### 5.2 与监督学习方法的对比
- **对比对象**：延世大学最佳模型（90%）
- **结果**：本文方法55% vs 监督学习90%
- **深入分析**：
  - 混淆矩阵对比：两类方法的错误模式有何不同？
  - 案例分析：监督学习做对的、本文方法做错的代码长什么样？
  - 案例分析：反过来，本文方法做对的、监督学习做错的代码？（如果存在）
- **讨论**：
  - 为什么有差距？监督学习从大量标注数据中学习到了超越简单运行模式的"深层特征"
  - 本文方法的优势：不需要训练数据，对新代码类型无偏

### 5.3 与大语言模型的对比
- **对比对象**：GPT-4o（62%）
- **结果**：本文方法55% vs GPT-4o 62%（差距7个百分点）
- **统计分析**：差异是否显著？（可做McNemar检验）
- **深入分析**：
  - 两者一致的样本（占多少？） → 这些是"简单案例"
  - GPT-4o对、本文方法错的样本 → GPT-4o凭借知识/记忆做对的？
  - 本文方法对、GPT-4o错的样本 → 需要实际执行才能判断的案例？
- **讨论**：
  - 专用轻量方法（本文）与通用大模型性能相当，但计算成本低几个数量级
  - 提示：也许两者结合能更好？（引出未来工作）

### 5.4 消融实验与敏感性分析
- **采样次数的影响**：采样5次 vs 10次 vs 20次 对准确率影响
- **噪声鲁棒性**：加入人工噪声，观察准确率下降情况
- **代码长度的影响**：短代码 vs 长代码的预测准确率

### 5.5 定性分析：可解释性案例研究
- 选几个典型案例，展示本文方法输出的函数表达式
- 解释这个表达式如何帮助理解代码性能
- 对比机器学习方法/LLM的输出（只是一个标签）

### 5.6 效率对比
- 预测耗时对比（表）
- 训练成本对比（本文无需训练，监督学习需GPU小时）
- 推理成本对比

---

## 6. 讨论

### 6.1 不同方法的适用场景
| 方法类型 | 优势 | 劣势 | 适用场景 |
|---------|------|------|---------|
| 理论分析 | 精确、通用 | 困难、需专家 | 教科书算法 |
| 本文经验方法 | 可解释、轻量、无数据依赖 | 准确率中等 | 资源受限、需解释、新代码类型 |
| 监督学习 | 高准确率 | 需大量标注数据、黑盒 | 有充足历史数据 |
| 大语言模型 | 通用知识、零样本 | 成本高、不稳定 | 快速原型、无专门模型 |

### 6.2 本方法的局限性
- 无法处理无法运行的代码（需要可执行环境）
- 对输入规模识别依赖启发式规则
- 对非数值输入规模（如字符串内容）处理困难
- 采样时间可能较长（尤其是对高复杂度代码）

### 6.3 对研究社区的启示
- 代码复杂度预测不是一个"一刀切"的问题
- 不同方法本质互补，未来研究可探索混合方法
- "准确率"不是唯一指标，可解释性、资源消耗同样重要

---

## 7. 结论与未来工作

### 7.1 结论
- 本文提出了一种基于运行时采样的经验性代码复杂度预测方法
- 实验表明，该方法（55%）虽不及监督学习（90%），但与GPT-4o（62%）相当
- 该方法在可解释性、低资源需求、无需训练数据方面具有独特价值
- 研究揭示了不同方法范式的优劣，为实际应用提供了选择依据

### 7.2 未来工作
- **混合方法探索**：用本文方法生成伪标签，辅助监督学习训练
- **采样策略优化**：引入贝叶斯优化等更智能的采样方法
- **扩展到更多复杂度类**：如O(mn)、O(n!)、O(n^m)等
- **处理非数值输入**：将输入特征编码纳入拟合
- **集成到开发工具**：做成IDE插件，实时提示代码复杂度

---

## 参考文献
（列出延世大学相关论文、经验算法学经典文献、LLM相关论文等）

---

## 附录（可选）
- 实验代码和数据的获取方式
- 详细实验结果表格
- GPT-4o提示词模板
- 典型案例的完整输出

---

# 论文写作要点提醒

1. **标题页**：包含标题、作者、单位、摘要、关键词
2. **图表**：每个章节尽量配流程图、对比图、结果图
3. **术语一致性**：全文统一称呼你的方法（如"经验拟合法"）
4. **语气**：自信但不傲慢，承认局限性但强调独特性
5. **数据支持**：每个结论都要有实验数据支撑

这个大纲把你的55%准确率从一个"尴尬点"变成了一个"有意义的发现"，让它成为论证"不同方法各有优劣"这一核心论点的重要证据。加油！    把这一部分大纲重新修正。