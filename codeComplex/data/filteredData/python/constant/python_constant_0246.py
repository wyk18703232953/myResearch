def main(n: int):
    # 根据规模 n 生成测试数据 m
    # 这里示例：m 取为 0 到 2^(n+1) 范围内的某个数
    # 你可以根据需要调整生成方式
    m = (n * 1234567) % (1 << (n + 1))

    # 原逻辑：输出 m % (2^n)
    print(m % (1 << n))


if __name__ == "__main__":
    # 示例调用：可根据需要修改 n 的值做测试
    main(5)