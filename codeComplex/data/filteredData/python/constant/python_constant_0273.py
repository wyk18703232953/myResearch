def main(n):
    # 这里根据 n 生成测试数据，本题原逻辑只需要一个整数输入
    test_n = n

    # 原逻辑：输入 n，输出 (n // 2) + 1
    result = (test_n // 2) + 1
    print(result)


if __name__ == "__main__":
    # 示例：调用 main(10)
    main(10)