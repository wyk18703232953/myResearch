def main(n):
    # 根据规模 n 生成测试数据 m
    # 这里示例为：m 为 0 到 2^(n+1)-1 的某个数
    # 可以按需修改生成规则
    m = (1 << (n + 1)) - 1

    # 原逻辑：输出 m % 2**n
    print(m % (1 << n))


if __name__ == "__main__":
    # 示例：调用 main(10)
    main(10)