def main(n):
    # 原程序逻辑：读取一个整数 x，输出 (x // 2) * 3
    # 这里将输入规模 n 映射为原程序的单个输入整数 x = n
    x = n
    result = (x // 2) * 3
    # print(result)
    pass
    return result

if __name__ == "__main__":
    # 示例调用：可以根据需要修改 n 的值
    main(10)