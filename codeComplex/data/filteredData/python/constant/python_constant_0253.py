def main(n: int):
    # 根据规模 n 生成测试数据 m
    # 示例：生成一个与 2^n 同数量级的整数
    # 你也可以根据实际需要自行调整生成规则
    m = (1 << (n + 1)) + 123456789

    # 原逻辑：输出 m % (1 << n)
    result = m % (1 << n)
    print(result)


if __name__ == "__main__":
    # 示例调用，可根据需要修改 n
    main(10)