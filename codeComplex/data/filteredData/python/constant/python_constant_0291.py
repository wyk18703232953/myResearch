import random

def main(n):
    # 根据规模 n 生成测试数据，这里直接用 n 作为原逻辑中的输入
    x = n
    # 原逻辑：输出 (x + 2) // 2
    result = (x + 2) // 2
    print(result)

if __name__ == "__main__":
    # 示例：可在此处指定规模测试
    main(10)