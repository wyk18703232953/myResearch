def main(n: int):
    # 根据规模 n 生成测试数据，这里约定：
    # a = n, b = n + 1（原程序中 b 未使用）
    a = n
    b = n + 1  # 保留与原输入结构一致，但不使用

    # print("1" * a)
    pass
    # print(int("1" + ("0" * a)) - int("1" * a))
    pass
if __name__ == "__main__":
    # 示例调用，可根据需要修改 n
    main(5)