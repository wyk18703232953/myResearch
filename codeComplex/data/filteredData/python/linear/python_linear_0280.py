def main(n):
    # 解释：
    # 原程序输入：n, m, a, b
    # 为了做规模实验，这里用单个参数 n 来确定性地产生这四个值
    # 让输入规模与 n 本身相关，同时保证无随机性

    if n <= 0:
        # 对于非正规模，返回一个固定结果，避免除以零等问题
        # print(0)
        pass
        return

    # 确定性构造 m, a, b，且保证 m > 0
    m = n + 1          # 始终使 m 与 n 同阶，且 m > 0
    a = n * 2 + 3      # 简单线性关系
    b = n * 3 + 5      # 简单线性关系

    # 保持原算法逻辑不变
    result = min(n % m * b, (m - n % m) * a)
    # print(result)
    pass
if __name__ == "__main__":
    # 示例调用：可根据需要修改 n 的值做时间复杂度实验
    main(10)