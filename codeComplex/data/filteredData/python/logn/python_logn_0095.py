def main(n: int):
    # 根据规模 n 生成测试数据：
    # 这里约定：l, r 为 [0, 2^n - 1] 内的两个整数
    # 为了构造一个有代表性的测试，用最极端的区间 [0, 2^n - 1]
    l = 0
    r = (1 << n) - 1

    # 原始逻辑
    ans = (1 << ((l ^ r).bit_length())) - 1

    print(ans)


if __name__ == "__main__":
    # 示例：可自行修改 n 做简单测试
    main(5)