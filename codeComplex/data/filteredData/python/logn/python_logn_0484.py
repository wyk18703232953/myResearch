def main(n):
    mod = 10**9 + 7  # 保留原代码中的定义（本题中未实际使用）

    x = 1
    i = 0

    # 找到位数区间
    while x <= n:
        x += 9 * (i + 1) * (10 ** i)
        i += 1

    # 回退到正确的区间起点
    x -= 9 * i * (10 ** (i - 1))

    # 找到包含第 n 位的具体数字
    y = str(10 ** (i - 1) + (n - x) // i)

    # 输出第 n 位对应的数字
    print(y[(n - x) % i])


if __name__ == "__main__":
    # 示例：生成一个测试规模 n 并运行
    # 这里简单地用一个固定的 n，也可以根据需要改为其他规则
    test_n = 1000
    main(test_n)