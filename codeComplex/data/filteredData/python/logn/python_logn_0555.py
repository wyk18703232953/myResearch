def main(n):
    # n 为规模，这里将第 n 个数字（从 1 开始计数）作为问题输入
    # 若希望批量测试，可以在底部自行循环调用 main(i)
    k = n - 1  # 原代码中读入后立刻 -1，转换为从 0 开始的索引

    x = 1
    y = 9
    # 找到第 k 位所在的位数区间
    while k > x * y:
        k -= x * y
        y *= 10
        x += 1

    a = 10 ** (x - 1)      # 当前区间的起始数字
    a += k // x            # 第 k 位所在的具体数字
    digit = str(a)[k % x]  # 在该数字中的具体位

    # print(digit)
    pass
if __name__ == "__main__":
    # 示例：调用 main(n)，n 为要查询的第 n 个数字位置
    # 这里给出一个示例测试，可以根据需要修改或批量生成 n
    test_n = 100
    main(test_n)