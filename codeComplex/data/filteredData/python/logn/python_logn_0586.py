import math

def main(n):
    # 解释输入结构：
    # 原代码读取: n, k = ria()
    # 这里将实验规模参数也命名为 n，
    # 并构造一个与规模相关的 k，使得 (b^2 - 4ac) 为非负。
    #
    # 令:
    #   a = 1
    #   b = -(2n + 3)
    #   c = n(n+1) - 2k
    # 判别式:
    #   D = b^2 - 4ac = (2n+3)^2 - 4*(n(n+1) - 2k)
    #                 = 8n + 9 + 8k
    # 只要 k >= 0 则 D >= 9 > 0，一定非负。
    #
    # 为了让 k 随规模变化，同时保持确定性，这里取:
    #   k = n^2
    # 这样输入规模 n 就是原问题里的 n，k 是 n 的确定性函数。

    k = n * n

    a = 1
    b = -(2 * n + 3)
    c = n * (n + 1) - 2 * k

    disc = b * b - 4 * a * c
    x = (-b - int(math.sqrt(disc))) // (2 * a)

    # 保持与原逻辑等价的输出
    # print(x)
    pass
if __name__ == "__main__":
    # 示例调用：可以修改为任意希望测试的规模
    main(10)