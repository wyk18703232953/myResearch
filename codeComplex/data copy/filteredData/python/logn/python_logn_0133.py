def main(n):
    # 生成规模为 n 的测试数据：
    # 保持原问题语义：原程序中读入的是 n, k，然后做 n-1, k-1
    # 这里将“逻辑中的 n”设为 n-1，“逻辑中的 k”设为 n（可根据需要调整生成规则）
    N = n - 1          # 原代码中处理用的 n
    k = n              # 原代码中处理用的 k （这里简单设置为 n）

    # 原始逻辑开始（此时 N 相当于原代码的 n-1，k 相当于原代码的 k-1 后被重新命名）
    N, K = N, k
    N, K = N - 1, K - 1  # 对应原代码：n, k = n-1, k-1

    l = 0
    r = K
    g = K * (K + 1) // 2
    ans = -1

    while l <= r:
        m = (l + r) // 2
        if g - m * (m + 1) // 2 >= N:
            ans = K - m
            l = m + 1

        else:
            r = m - 1

    # print(ans)
    pass
if __name__ == "__main__":
    # 示例调用：可根据需要修改 n 的测试规模
    main(10)