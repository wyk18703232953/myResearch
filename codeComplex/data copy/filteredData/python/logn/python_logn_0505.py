def main(n: int):
    # 这里的 n 作为规模参数，用来生成一个测试用的 k
    # 原题逻辑本身只依赖于单个整数 k，这里构造一个不超过 10**11 的 k
    # 你可以根据需要修改生成规则
    k = (n * n * 1234567) % (10**11)

    T = (
        0,
        9,
        189,
        2889,
        38889,
        488889,
        5888889,
        68888889,
        788888889,
        8888888889,
        98888888889,
        1088888888889,
    )

    a = 0
    for i in T:
        if i - k > 0:
            a = T.index(i)
            break

    temp = T[a] - k
    x = temp % a
    res = (10 ** a) - 1 - int(temp / a)
    ans = int((res % (10 ** (x + 1))) / (10 ** x))
    # print(ans)
    pass
if __name__ == "__main__":
    # 示例：调用 main，n 可按需修改
    main(1000)