def main(n):
    # 这里根据 n 生成测试数据：构造一个合理的 k
    # 为了让程序有解，我们可以人为构造一个 i0（0 <= i0 <= min(n, 10**5-1)）
    # 然后根据原方程反推 k：
    #   (i*(i+1))//2 - (n - i) = k
    # 令 i = i0，则：
    #   k = (i0*(i0+1))//2 - (n - i0)
    # 这样在枚举时一定会找到 n - i0 作为输出。
    i0 = min(n // 2, 10**5 - 1)  # 任意选择一个 i0，保证在枚举范围内
    k = (i0 * (i0 + 1)) // 2 - (n - i0)

    # 原逻辑：在 0..10**5-1 中找满足条件的 i，并输出 n - i
    for i in range(10**5):
        if (i * (i + 1)) // 2 - (n - i) == k:
            # print(n - i)
            pass
            break


if __name__ == "__main__":
    # 示例：调用 main，规模 n 可按需修改
    main(10**6)