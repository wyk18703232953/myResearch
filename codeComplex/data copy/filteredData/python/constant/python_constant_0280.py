def main(n):
    # 原程序逻辑：print(int(input()) // 2 + 1)
    # 这里将 n 作为输入规模，同时也作为原程序的输入值
    value = n
    result = value // 2 + 1
    # print(result)
    pass
if __name__ == "__main__":
    # 示例调用，可按需修改 n 以进行规模实验
    main(10)